# @tipjar/server

Embed server for TipJar. Serves the widget bundle, hosts the SEP-24 callback URL, and provides the creator dashboard's API surface.

The current state is a typed health stub; the Express implementation lands in a follow-up commit.
