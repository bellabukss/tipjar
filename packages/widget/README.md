# @tipjar/widget

The embeddable tipping widget. Renders as a `<tip-jar>` custom element and exposes a `mount()` function for programmatic use.

The current state is a typed surface; rendering and Stellar integration land in subsequent commits. See the root README for the product description.
