# @tipjar/demo

A static page that mounts the widget against a placeholder creator wallet, used for visual development and end-to-end verification.

Open `index.html` in a browser after building `@tipjar/widget`.
